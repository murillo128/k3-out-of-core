# Phase 13 iteration 3 — owner-range victim-selection comparator

Candidate: parent `d2b6cd664f7a2aa82eb2d966350b56e64298adce`, nested `6a9f225830736ccab04cabd37a4a020662c8f9a6`. Reverted by parent `9d3338a510811a90731504d7b823810b2c512d3d` and nested `6ed7b7de63516115f7c1bdc20d0cf78dd7233053` without rewriting published history.

`OBSERVED`: the hypothesis is falsified and the candidate is reverted. Hoisting global directory/policy validation and enumerating only each owner-device slot range changed two-pair B TPS by only +0.02% relative to Iteration 2. The screen measured A at 0.380046 tok/s, B at 0.242453 tok/s and A→B at 0.637957× (paired bootstrap 95% interval 0.623920–0.652607). Exact output identity held across all four fresh processes, with no storage, lifecycle, stale-generation, cancellation or resource residue.

The selected-window trace also missed the declared target. Relative to Iteration 2, B `pre_issue_ns` increased by 11.805%, B layer wall increased by 5.994%, and `post_issue_ns` changed by only +0.392%. The fresh A and B traces are valid, wall-exact and have SHA-256 values `ec621f377c3b2deeedb685c5577a9b38357eeb8c38746c54ac3228f90f51c2d5` and `db800e0a73a6e4216ef7c050c29c2bbe71711d5e5f71788e36d716e52c2a3c17`. Each contains 14 and 9 complete routed-layer cycles respectively.

Native compilation used `-j76`. The modified provider compiled and the 12 focused expert CTests passed sequentially. One transfer-ring test failed only during a 12-way GPU-contention run, then passed alone and in the complete sequential focused set. The global `all` target remains blocked by the pre-existing, out-of-scope `phase11-uma-probe` use of unavailable `STATX_DIOALIGN` fields in the host headers; exact Phase 13 targets build normally.

The remaining 39.067 ms/layer pre-plus-post provider delta is host policy work, not CUDA service, but the discarded owner-range mechanism does not explain it. Source sequencing exposes the next measurable candidate: every cache-policy event calls `hash_state()`, which walks every model key, policy slot and domain; B has 536 hot-policy slots while A has 268. Iteration 4 is instrumentation-only: add an exact trace scope around `hash_state()` without changing policy semantics. It predicts that extra B hash-state time will explain at least 20% of the remaining provider delta while quick-screen TPS stays within 3% of Iteration 2. State-digest optimization will be abandoned if that causal-share threshold is not met.
