# Phase 13 iteration 13 — persistent cross-device staging rejected; closeout target frozen

Candidate base: parent `9bd3e814065e6a5dbad0fe4b945dc19f34817279`, nested candidate `5c1334756e3d9a3167f09e4a3ff2203aea925d1f`. Reverted without rewriting history by nested `2c8afc4e1747bbd9a6ee93ca79ff645613a8ec07`; its source tree is exactly `7114b54d67540e580d15271dc230b02f585b16c7`, identical to retained Iteration 11.

The candidate replaced per-layer task creation with one provider-owned auxiliary worker only for a two-device provider. The request thread staged DeviceId 0 while the worker staged DeviceId 1; every call joined the worker before canonical scheduler/H2D finalization, including the primary-error path. Single-device A created no worker. Compilation of the changed source and exact targets used `-j76`; provider, peer-staging, hot-cache and async-I/O CTests passed. The whole-tree build again reached the unrelated pre-existing `phase11-uma-probe` host-header failure at `STATX_DIOALIGN`; the Phase-13 targets and `llama-cli` built successfully.

Mode-C B preserves the accepted full identity SHA-256 `60658621b12340bc02d1fbb614142e4a17c5dd52eb529bfd4b0b2eb1a1255889`, all 24 logit digests and 1,032 route records, with clean terminal lifecycle. Mode-P B is `1.414516` tok/s and preserves the accepted production-output identity `782383806379a7b5efcb36545a48bc2d82aa988b808d598a59b811302084f23b`.

`REJECTED`: B improves only `+0.345%` from the retained Iteration-11 Tier-1 reference (`1.409646` tok/s), far below the predeclared `+3%` first-stage threshold. Persistent task ownership removes Iteration 12's task-construction regression, but cross-ring host staging is not throughput-causal at the required magnitude. No retained-candidate trace or wider matrix is authorized and the candidate is reverted.

The latest design-authority amendment, issue comment `5226796421`, supersedes the old 1.60x goal with a preferred `>=0.95x A` closeout and requires optimization to stop when no removable in-scope component remains. Retained Iteration 11 already measures `0.977322x` in the adjacent Mode-P pair. Iteration 10 disproved the traced inactive-ID synchronization as a throughput-dominant bounded fix, and Iterations 12 and 13 disprove both ephemeral and persistent cross-device stage overlap as material throughput fixes. The retained provider is already `1.013 ms/layer` faster than A; the residual serialized graph belongs to the symmetric DeviceId-0 resident-graph topology, while generalized device roles are explicitly owned by follow-up issue #63.

The exact retained Iteration-11 source tree is therefore frozen for closeout. Next execution is the bounded Mode-C A/B qualification, the authoritative five-pair Mode-P A/B campaign, manifest/evidence refresh and one new final-capable independent review. B-prime is not repeated because the amendment makes it diagnostic only and no capacity hypothesis remains active.
